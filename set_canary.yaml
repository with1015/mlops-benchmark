apiVersion: v1
kind: Service
metadata:
  labels:
    app: myapp
  name: myapp-svc
  namespace: default
spec:
  ports:
    - name: http
      protocol: TCP
      port: 50001
      targetPort: 8080
  selector:
    app: myapp
  externalIPs:
    - 192.168.219.100


